frontend-next/node_modules/
.DS_Store
.tmp-dev/
# .claude/ 整个忽略，只放行团队共用的 settings.json。
# 注意是 .claude/* 而不是 .claude/：git 不会进到被忽略的目录里，
# 目录本身被忽略时后面的 ! 规则根本没机会生效。
.claude/*
!.claude/settings.json
.env
.env.*
runtime.env
# 生成的配置文件里有管理员密码和 API Key，示例是 config.example.yaml。
config.yaml
web-search.json
secrets/
*.pem
*.key
*.p12
*.pfx
cookies*.txt
*session*.json
napcat-qrcode*.png
dist/
logs/*
!logs/.gitkeep
data/*
*.log
